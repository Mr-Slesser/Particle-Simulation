#version 330 core

in vec4 v_colour;

out vec4 colour;

uniform float time;

void main()
{
    //colour = vec4(0.0f + sin(time * 0.25), 0.75f - sin(time * 0.5), 0.5f + cos(time), 1.0f);
    colour = v_colour;
}