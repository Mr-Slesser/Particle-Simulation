#version 330 core

layout (location = 0) in vec3 aPos;
layout (location = 1) in vec4 aCol;

out vec4 v_colour;

uniform float time;

void main()
{
    v_colour = aCol;
    gl_Position = vec4(aPos.x / 2 - sin(time * 0.1), aPos.y / 2 - cos(time * 0.1), aPos.z, 1.0);
}