// #version 330 core

// in float inValue;
// out float outValue;

// void main()
// {
//     outValue = inValue + 1;
// } 

#version 330 core

layout (location = 0) in vec3 aPos;
layout (location = 1) in vec4 aColor;

out vec3 outPosition;
out vec4 outColor;

void main()
{
    //outPosition = aPos + vec3(-0.1, -0.1, 0.0);
    //outColor = aColor + vec4(0.01, -0.01, 0.01, 0.0); 
    outPosition = aPos;
    outColor = aColor;
} 